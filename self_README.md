This respository stores my .config/nvim files

go to .config then git clone (might need to delete nvim/ folder before git clone works)
need to checkout the correct branch tho, each branch for a different nvim starter (self / nvchad / lazy)
